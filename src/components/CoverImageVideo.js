import { Box } from "@chakra-ui/react";
import React, { useState } from "react";

const CoverImageVideo = () => {
  const [isPlayable, setPlayable] = useState(false);

  return <Box></Box>;
};

export default CoverImageVideo;
